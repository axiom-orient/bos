# Tasks (Execution Queue)

## Active Queue
| TASK-ID | Priority | Status | Description | Done Criteria | Evidence |
|---|---|---|---|---|---|
| REL-011 | P0 | TODO | first writable signing seed를 실제 target 또는 disposable app에 대해 수집하고 최종 출시 판정을 닫는다 | `app-register -> release-run --stage build --allow-signing-write` 또는 `release-check --mode sync-certs --allow-write`가 실제로 재현되고 artifacts/state evidence가 남음 | temp workspace logs, `.bos/artifacts/**`, `.bos/state/bos.state.yaml` |

## Completed Baseline
| TASK-ID | Priority | Status | Description | Done Criteria | Evidence |
|---|---|---|---|---|---|
| CFG-010 | P0 | DONE | `.bos/config/profile.yaml`를 onboarding non-secret SSOT로 확장 | `companyName`, `appName`, `appIdentifier`, `appleTeamId`, `primaryLanguage`, `sku`, `matchGitURL`를 optional/additive로 저장하고 기존 profile decode를 깨지 않음 | `Sources/BosCore/Schemas.swift`, `Sources/BosCore/OnboardingConfiguration.swift`, `Sources/BosCLI/main.swift`, `Tests/CoreTests/SchemaValidationTests.swift`, `swift test` (85 tests, 0 failures; 2026-03-09 KST) |
| CFG-011 | P0 | DONE | `MATCH_GIT_URL`를 profile SSOT로 이동하고 signing env는 secret-only boundary로 재정렬 | `release-init` / `release-check`가 profile의 `matchGitURL`를 읽고, legacy signing env fallback도 유지 | `Sources/BosCLI/main.swift`, `Sources/BosCore/ReleaseInitEngine.swift`, `Sources/BosCore/ReleaseCheckEngine.swift`, `Tests/CoreTests/ReleaseInitEngineIntegrationTests.swift`, `Tests/CoreTests/ReleaseCheckEngineIntegrationTests.swift`, `swift test` (85 tests, 0 failures; 2026-03-09 KST) |
| PLAN-010 | P0 | DONE | PlanEngine이 app onboarding metadata를 profile 우선순위로 해석하도록 확장 | profile에 값이 있으면 PRD에서 `App Identifier`, `Apple Team ID`가 빠져도 blueprint 생성이 가능하고, CLI/PRD explicit 값은 profile보다 우선 | `Sources/BosCore/PlanEngine.swift`, `Tests/CoreTests/PlanEngineIntegrationTests.swift`, `swift test` (85 tests, 0 failures; 2026-03-09 KST) |
| PLAN-011 | P1 | DONE | Blueprint release metadata를 app registration에 필요한 필드까지 additive 확장 | `Blueprint.release.fastlane`에 `appName`, `sku`, `primaryLanguage`, `companyName`가 추가되고 기존 blueprint decode와 apply/release-init 경로가 유지 | `Sources/BosCore/Schemas.swift`, `Tests/CoreTests/SchemaValidationTests.swift`, `Tests/CoreTests/PlanEngineIntegrationTests.swift`, `swift test` (85 tests, 0 failures; 2026-03-09 KST) |
| APP-010 | P0 | DONE | `bos app-register` CLI 계약과 JSON/human 출력 surface를 고정 | `app-register` usage/help, parse rules, required input validation, result payload가 테스트와 문서에 동일하게 반영 | `Sources/BosCLI/main.swift`, `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift`, `swift test --filter CLIJsonOutputIntegrationTests` (22 tests, 0 failures; 2026-03-09 KST) |
| APP-011 | P0 | DONE | app registration backend feasibility를 결정하고 adapter 뒤에 숨긴다 | `bos app-register`가 fastlane/backend 세부사항을 노출하지 않고, 선택된 provider 한 개가 safe live path까지 검증 | `Sources/BosCore/AppRegistrationEngine.swift`, `docs/IMPLEMENTATION-PLAN.md` DG-2, `/tmp/bos-live-app-register-iVfLZ8/.bos/artifacts/app-register/app-register-20260309041242.log` (`existing/existing`) |
| APP-012 | P0 | DONE | deterministic SKU 생성 규칙과 auto-backfill을 구현 | `sku` 미입력 시 `companySlugOrBundleSeed.appSlug.hash8(appIdentifier)` 규칙으로 결정적 생성, explicit override 허용, profile backfill 가능 | `Sources/BosCore/AppRegistrationEngine.swift`, `Sources/BosCore/PlanEngine.swift`, `Tests/CoreTests/AppRegistrationIntegrationTests.swift`, `Tests/CoreTests/PlanEngineIntegrationTests.swift`, `swift test` (66 targeted tests, 0 failures; 2026-03-09 KST) |
| APP-013 | P1 | DONE | `doctor --for app-register` preflight를 추가해 onboarding gate를 독립 표면으로 고정 | `doctor`가 App Store Connect secret만 required로 검사하고 release scope와 분리된다 | `Sources/BosCLI/main.swift`, `Sources/BosCore/DoctorEngine.swift`, `Tests/CoreTests/DoctorEngineIntegrationTests.swift`, `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift`, `/tmp/bos-live-app-register-iVfLZ8/.bos/artifacts/doctor/doctor-20260309042612.json`, `swift test --filter DoctorEngineIntegrationTests`, `swift test --filter CLIJsonOutputIntegrationTests` (2026-03-09 KST) |
| REL-010 | P1 | DONE | `release-init` / `release-check`가 new SSOT를 읽도록 연결 | profile 기반 onboarding metadata와 legacy inputs가 함께 동작하고, signing secret만 env 파일에 남는다 | `Sources/BosCore/ReleaseInitEngine.swift`, `Sources/BosCore/ReleaseCheckEngine.swift`, `Tests/CoreTests/ReleaseInitEngineIntegrationTests.swift`, `Tests/CoreTests/ReleaseCheckEngineIntegrationTests.swift`, `swift test` (85 tests, 0 failures; 2026-03-09 KST) |
| REL-012 | P0 | DONE | `bos release-run` product surface를 추가해 IPA build/upload/submit를 fastlane 위에서 단일 계약으로 래핑 | `release-run` CLI/help/JSON, stage(`build|beta|release|submit`), internal `release-init + release-check + tuist + fastlane` pipeline, artifact/state sync가 동작 | `Sources/BosCLI/main.swift`, `Sources/BosCore/ReleaseRunEngine.swift`, `Sources/BosCore/ReleaseInitEngine.swift`, `Tests/CoreTests/ReleaseRunEngineIntegrationTests.swift`, `swift test --filter ReleaseRunEngineIntegrationTests` (3 tests, 0 failures; 2026-03-09 KST) |
| REL-013 | P0 | DONE | release build path의 근본 원인인 generated workspace/scheme/signing drift를 명령 수준에서 고정 | `verify`와 `release-run`이 동일한 scheme resolution과 debug signing suppression을 사용하고 기존 generated repo도 deterministic하게 검증 가능 | `Sources/BosCore/ProjectBuildSupport.swift`, `Sources/BosCore/VerifyEngine.swift`, `Tests/CoreTests/VerifyEngineIntegrationTests.swift`, `Tests/CoreTests/ProfilePolicyE2ETests.swift`, `swift test --filter VerifyEngineIntegrationTests --filter ProfilePolicyE2ETests` (2026-03-09 KST) |
| APP-014 | P1 | DONE | 기존 repo layout에서 `.bos/plan/blueprint.yaml`가 없어도 `config/blueprint.yaml` fallback으로 product path를 유지 | `app-register`, `release-init`, `release-run`, `apply`가 공통 blueprint resolution을 사용하고 existing repo를 별도 flag 없이 처리 | `Sources/BosCLI/main.swift`, `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift`, `swift test --filter CLIJsonOutputIntegrationTests` (2026-03-09 KST) |
| QA-010 | P0 | DONE | onboarding SSOT와 app registration 회귀를 자동 테스트로 고정 | schema/preference/CLI/engine/live contract가 테스트로 재현 가능 | `Tests/CoreTests/PlanEngineIntegrationTests.swift`, `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift`, `Tests/CoreTests/AppRegistrationIntegrationTests.swift`, `Tests/CoreTests/DoctorEngineIntegrationTests.swift`, `swift test` (66 targeted tests, 0 failures; 2026-03-09 KST) |
| DOC-010 | P1 | DONE | README / Product Guide / Testing Guide를 새 onboarding 원칙에 맞게 갱신 | “team-level SSOT + per-app onboarding + explicit signing seed” 흐름이 문서 전반에 일관되게 반영 | `README.md`, `docs/PRODUCT_GUIDE.md`, `docs/TESTING_GUIDE.md`, `Tests/README.md` |
| REL-005 | P0 | DONE | `bos release-check` CLI 계약, mode semantics, `--allow-write` safety gate를 고정 | `bos release-check` usage/help, default mode, invalid flag/guard behavior가 테스트와 문서에 동일하게 반영 | `Sources/BosCLI/main.swift`, `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift` (`releaseCheckRejectsWriteModeWithoutExplicitAllowWrite`, `releaseCheckReturnsParseableJSONOnExecutionFailure`), `swift test --filter CLIJsonOutputIntegrationTests` (22 tests, 0 failures; 2026-03-09 KST) |
| REL-006 | P0 | DONE | `release-check`를 toolchain/state/artifact surface에 additive 방식으로 연결 | `ToolchainLock`, `DoctorScope`, `BosStateStore`, `.bos/state/bos.state.yaml`이 기존 workspace를 깨지 않고 `releaseCheckSummary`를 지원 | `Sources/BosCore/Schemas.swift`, `Sources/BosCore/BosStateStore.swift`, `Sources/BosCore/ApplyEngine.swift`, `Tests/CoreTests/SchemaValidationTests.swift`, `Tests/CoreTests/DoctorEngineIntegrationTests.swift` |
| REL-007 | P0 | DONE | 실제 App Store Connect / `match` reachability를 검증하는 `ReleaseCheckEngine`를 구현 | `connectivity`, `readonly-certs`, `sync-certs` mode가 step ordering과 failure taxonomy를 포함해 동작 | `Sources/BosCore/ReleaseCheckEngine.swift`, `Tests/CoreTests/ReleaseCheckEngineIntegrationTests.swift`, `swift test --filter ReleaseCheckEngineIntegrationTests` (6 tests, 0 failures; 2026-03-09 KST) |
| REL-008 | P0 | DONE | `release-init` scaffold를 `release-check`에 필요한 read-only/write lane 구조로 확장 | generated `Fastfile`에 `auth_ping`, `certs_readonly`, `certs` lane 계약이 생기고 `release-check`가 이를 재사용 | `Sources/BosCore/ReleaseInitEngine.swift`, `Tests/CoreTests/ReleaseInitEngineIntegrationTests.swift`, `swift test --filter ReleaseInitEngineIntegrationTests` (5 tests, 0 failures; 2026-03-09 KST) |
| QA-008 | P0 | DONE | 자동 테스트로 CLI 계약, failure 분류, state sync, secret redaction 회귀를 막기 | `swift test`만으로 외부 시스템 없이도 release-check 회귀를 재현 가능 | `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift`, `Tests/CoreTests/ReleaseCheckEngineIntegrationTests.swift`, `Tests/CoreTests/DoctorEngineIntegrationTests.swift`, `Tests/CoreTests/SchemaValidationTests.swift`, `swift test` (85 tests, 0 failures; 2026-03-09 KST) |
| QA-009 | P1 | DONE | 실제 secret를 쓰는 opt-in live verification runbook과 temp workspace 시나리오를 만든다 | 전용 credential을 사용해 `doctor --for release-check -> release-init -> release-check`를 실제 폴더에서 재현하는 문서/체크리스트가 존재 | `docs/TESTING_GUIDE.md` (`Release-Check Commands`, `Live Runbook`) |
| DOC-004 | P1 | DONE | 사용자 문서에서 `verify`, `release-init`, `release-check`의 경계를 명확히 설명 | README / Product Guide / Testing Guide가 smoke 검증, scaffold 생성, live signing 검증을 혼동 없이 설명 | `README.md`, `docs/PRODUCT_GUIDE.md`, `docs/TESTING_GUIDE.md`, `Tests/README.md` |
| REL-009 | P0 | BLOCKED | 구현 완료 후 실제 release evidence를 수집하고 출시 가능 여부를 재판정 | 자동 테스트 green + opt-in live run 성공 + artifacts/state evidence가 모여 최종 release verdict를 낼 수 있음 | 현재 release-check live auth와 match reachability는 확인됐다. empty repo 기준 first writable seed는 아직 수행 전 |
| QA-006 | P0 | DONE | `swift test`에서 `Testing` 모듈 오류가 재현되지 않도록 테스트 런타임 의존성을 패키지 선언에 고정 | `CoreTests`가 `Testing` product를 명시적으로 의존하고 `swift test`가 green | `Package.swift`, `Package.resolved`, `swift test` (64 tests, 0 failures; 2026-03-09 KST) |
| QA-007 | P0 | DONE | `doctor` 기본 scope와 explicit `release-init` scope를 테스트 이름/의도/검증 항목에 분리 반영 | 기본 실행은 `core`, signing template 생성은 `--for release-init`에서만 검증 | `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift` (`doctorDefaultsToCoreAndSkipsSigningTemplateBootstrap`, `doctorReleaseInitScopeCreatesSigningEnvTemplate`), `swift test --filter CLIJsonOutputIntegrationTests` (19 tests, 0 failures; 2026-03-09 KST) |
| LEG-004 | P1 | DONE | legacy `schemaVersion: 1` toolchain lock 실패를 migration 메시지로 명확화 | 사용자가 decode stacktrace 대신 재생성 안내를 받는다 | `Sources/BosCLI/main.swift` (`decodeToolchainLock`), `Tests/CoreTests/CLIJsonOutputIntegrationTests.swift` (`doctorReportsClearMigrationErrorForLegacyToolchainLockSchema`) |
| DOC-003 | P1 | DONE | 테스트 실행 노하우와 Xcode-sensitive 명령 실행법을 문서화 | `Testing` 오류 원인, `DEVELOPER_DIR` 사용법, doctor/release-init scope 차이가 문서에 반영 | `docs/TESTING_GUIDE.md`, `README.md`, `docs/PRODUCT_GUIDE.md`, `Tests/README.md` |
| REL-004 | P0 | DONE | 오늘 기준 출시 게이트를 다시 실행해 현재 코드 기준으로 판정 갱신 | CLI 테스트, 전체 회귀, 릴리즈 빌드, 실제 `doctor` 샘플 실행이 모두 통과 | `swift test --filter CLIJsonOutputIntegrationTests`, `swift test`, `swift build -c release`, `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer BOS_AUTO_INSTALL=0 .build/debug/bos doctor --project-root <tmp> --format json` |

## Deferred / Out of Scope
- CI secrets orchestration
- multi-app workspace orchestration
